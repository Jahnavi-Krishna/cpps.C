#include<stdio.h>
int input(int*a,int*b,int *c,int*d)
{       
        printf("Enter the numerator of the first fraction");
}
int main()
{
        int w,x,y,z;
        input(&w,&x,&y,&z);
}


#include<stdio.h>
struct fraction
{
        int num;
        int den;
};      
void get fraction( struct fraction )
