Day 3 [Comparing 2 numbers](11 Learning Episodes from Comparing 2 numbers + 3 Learning Episodes in Smallest of 3 numbers)
{COMPARING 2 NUMBERS}
#include<stdio.h>
int input(int*a,int*b)
{
        printf("Enter the first number\n");
        scanf("%d",a);
        printf("Enter thr second number\n");
        scanf("%d",b);
}
int compare(int m,int n)
{
        int p;
        if(m==n)
                p=00;

        else
        {
                if(m<n)
                        p=m;
                else
                        p=n;
        }
        return p;
}
void output(int q,int r,int s)
{
        if(s==00)
                printf("Both are equal");

        else
                printf("%d is smaller among %d and %d",s,r,q);

}
int main()
{
        int x,y,z;
        input(&x,&y);
        z=compare(x,y);
        output(x,y,z);
}
