 Day 3 Continued[Comparing 3 numbers](11 Learning Episodes from comparing 2 numbers + 3 Learning Episodes in Smallest of 3 numbers)
 {COMPARING 3 NUMBERS}Ladder if
 #include<stdio.h>
int input(int*a,int*b,int*c)
{
        printf("Enter the first number\n");
        scanf("%d",a);
        printf("Enter thr second number\n");
        scanf("%d",b);
        printf("Enter thr third number\n");
        scanf("%d",c);
}
int compare(int m,int n,int o)
{
        if(m<n&&m<o)
                return m;
        else if(n<m&&n<o)
                return n;
        else
                return o;
}
void output(int q,int r,int s,int t)
{
        printf("%d is smallest among %d , %d and %d",t,q,r,s);

}
int main()
{
        int w,x,y,z;
        input(&w,&x,&y);
        z=compare(w,x,y);
        output(w,x,y,z);
}
